@extends('layouts.staff')
@section('title', 'Gallery')
@section('content')
<div class="page-head"><h1>Gallery <span>Management</span></h1><a href="{{ route('staff.gallery.create') }}" class="btn btn-honey"><i class="fa-solid fa-upload"></i> Upload image</a></div><div class="row g-4">@forelse($images as $image)<div class="col-sm-6 col-lg-4"><section class="honey-card p-0 overflow-hidden h-100"><img src="{{ asset('storage/'.$image->image) }}" alt="{{ $image->title ?? $image->category }}" class="w-100" style="height:200px;object-fit:cover"><div class="p-3"><strong>{{ $image->title ?: 'Untitled image' }}</strong><p class="text-muted small mb-3">{{ $image->category }}</p><a href="{{ route('staff.gallery.edit',$image->id) }}" class="btn btn-sm btn-outline-dark">Edit</a><form class="d-inline" method="POST" action="{{ route('staff.gallery.destroy',$image->id) }}" onsubmit="return confirm('Delete this gallery item?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></section></div>@empty <div class="col-12"><section class="honey-card text-center text-muted py-5">No gallery images have been uploaded yet.</section></div>@endforelse</div>
@endsection
